

def Unification(data):
	text = ''
	for word in data:
		text += word[0] + word[1] + ' '
	return text

# Формат data: [<слово>, <знак препинания после него>, <часть речи>, <часть предложения>]

def PunctuationFormatter(data):
	return data
	if data == 'Субтитры отсутствуют':
		return data

	#print(data)
	return Unification(data)
