alphabet = 'абвгдеёжзийклмнопрстуфхцчшщьыъэюя'
alphabet2 = 'abcdefghijklmnopqrstuvwxyz'

def LettersIntoInputWeights(text):
	weights = []
	for l in text:
		num = 0
		if l in alphabet:
			num = (alphabet.rindex(l) + 1) / 33
		elif l in alphabet2:
			num = (alphabet2.rindex(l) + 1) / 26
		weights.append(num)

def DeleteUsefulInformation():
	

def NormalizeText():
